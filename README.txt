설치. pip install wordcloud
설치. pip install konlpy // 한국어 자연어 처리 라이브러리
설치. pip install matplotlib
설치. pip install flask
설치. pip install flask_cors

정적파일. 이미지 파일에 쉽게 접근할 수 있어야함.
정적파일. 이미지 파일을 클라이언트가 접근할 수 있도록 하려면 정적폴더가 필요하다.
정적파일. 클라이언트가 바로 접근가능하다.